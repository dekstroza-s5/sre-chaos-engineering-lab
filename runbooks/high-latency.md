# High latency

Compare p50, p95 and p99, request rate, CPU throttling, memory pressure and downstream latency. Determine whether queuing or saturation is present.

Reduce load or scale only after identifying the constrained resource. Confirm latency recovers and the error rate remains stable.
